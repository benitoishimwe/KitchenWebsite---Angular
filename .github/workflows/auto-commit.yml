name: Auto Commit

on:
  schedule:
    - cron: "0 10 * * *"   # runs every day at 10:00 UTC
  workflow_dispatch:        # allows manual run

permissions:
  contents: write

jobs:
  auto-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Make changes
        run: |
          echo "Updated at $(date)" >> auto-update-log.txt

      - name: Commit changes
        run: |
          git config --global user.name "benitoishimwe"
          git config --global user.email "73600412+benitoishimwe@users.noreply.github.com"
          git add .
          git commit -m "Automated commit" || echo "No changes to commit"
          git push
